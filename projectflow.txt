1. Create a git repo and clone it in local 
2. create code.py and add code to it.
3.doa git add-cmmit push before initializing DVC
4. now we do dvc init(create.dvcginore.dvc)
5.now we do 'mkdir s3'(creates a new s2 directory)
6.Now we do 'dvc remote add -d myremote S3'
7.next 'dvc add data/'